# 设计系统

语义色由 `globals.css` 统一定义：背景、表面、文字、弱文字、边框、品牌色、成功、警告和危险。业务组件不得自行引入第二强调色。

按钮分为主要、次要、幽灵和危险四种；按钮文案在桌面端不换行，按下态有轻微位移。焦点环始终可见。卡片只用于任务、反馈和需要独立语义的区域，不用于把每段文字装盒。

工作台数值和命令使用等宽字体；正文最小 16px；触摸目标最小 44×44px；内容容器最大宽度 1280px。层级只使用 `base / sticky / overlay / modal` 四档。
