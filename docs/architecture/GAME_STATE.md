# 游戏状态

`GameState` 保存存储版本、辅助模式、当前章节、已完成章节、每道任务尝试、已掌握 P0、开始时间和累计用时。所有变化通过纯 reducer 事件完成：加载、选择章节、回答、完成章节、切换辅助、重置。

章节完成不要求一次答对。错误反馈解释原因并允许立即重试。真实章节使用 self-check，不显示自动验证文案。主线毕业要求第 0-13 章完成且 P0 掌握率 100%。
