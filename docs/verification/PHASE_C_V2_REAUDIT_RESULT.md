# 阶段 C · V2 复核结果

日期：2026-09-14。用户已完成人工核验，阶段 C V2 标记为“人工核验通过”。这不等同于已上传 GitHub、已发布或已完成 Apple 公证。

## 已交付

- 地图使用独立生成的 3:2 桑皮纸 PNG：毛边、缺口、折痕、晕染和卷角；山水仍按原构图叠放。标题仅显示“云溪谷”，14 个地图节点（0–12 主线及百戏客栈）共用同一坐标网格。
- 13 章主线的 104 件既有素材按 alpha 连通物体重切为透明 PNG，另补纸张、客栈锁卷轴和骨骰；第四、十章卡槽与托盘统一图片、标题、状态轨道。
- 山道连连看使用实际端点中心的 SVG 墨色虚线；错误匹配不留线，撤销和重置清线。
- 翻书过场保留静态左页，以中央书脊为轴翻动单张右页，带正反面、跳过和减少动态。
- “百戏客栈 · 六骰聚财”从地图右下锁卷轴进入。三回合、5/10/20 文虚拟押注；每次自行留骰、收分或续掷，失手丢失未收分；真实状态机结算称号和返还，成绩卡由本轮分数生成 PNG。银票只兑换青玉桌装饰，不改概率。
- 主线首次奖励、重玩收益、章内详解、银票和账本均通过版本化本机存档；中断中的客栈局可恢复，结算只记一次。没有支付、账号、广告或真实赌博。

## 复核证据

| 检查 | 结果 |
|---|---|
| `npm run content:validate` / `content:coverage` | 123 个术语，P0/P1 主线覆盖 100% |
| `npm run typecheck` / `lint` | 通过；lint 仅 3 条既有 `docs/design` 警告 |
| `npm run test` | 121 tests / 16 files passed |
| `npm run desktop:test` | 6 passed |
| `npm run assets:validate` | 169 个运行资产通过；1 个未使用历史品牌图的既有 provenance gap 被排除出分发 |
| `npm run build` / `npm run desktop:build` | 通过，静态导出与 Electron 构建指纹一致 |
| 浏览器复核 | `artifacts/remediation-v2/report.json`：24 张截图、13 章、4 路连线、客栈结算、PNG 导出、1440/768 窄宽、10 次翻页、减少动态，`errors: []` |
| 新打包 Electron 复核 | `artifacts/remediation-v2-native-final/report.json`：最新自定义图标包同一流程，另验证 PNG 实际落盘，`errors: []` |

C V2 历史桌面预览包已在 2026-09-14 清理；当前候选包与验收状态见 [阶段 D 成品验收](../releases/PHASE_D_ACCEPTANCE_2026-09-14.md)。

上述完整主线存档由 `campaignSolution` 逐章重放生成，仅用于独立测试，不读取或修改玩家正式存档。

## 验收结论

用户已确认纸张地图、卡片布局、翻页效果、素材清理、主线与支线玩法及行囊经济循环均通过人工核验。当前没有发布或上传 GitHub；桌面包为本机 ad-hoc 签名预览，尚未完成 Apple 公证。
